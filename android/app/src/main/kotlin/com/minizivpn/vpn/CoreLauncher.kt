package com.minizivpn.vpn

class CoreLauncher {

    fun startCore(configPath: String) {
        println("Starting VPN core with config: $configPath")
    }

    fun stopCore() {
        println("Stopping VPN core")
    }
}
